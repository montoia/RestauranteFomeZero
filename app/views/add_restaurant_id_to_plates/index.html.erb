<p id="notice"><%= notice %></p>

<h1>Add Restaurant Id To Plates</h1>

<table>
  <thead>
    <tr>
      <th>Restaurant</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_restaurant_id_to_plates.each do |add_restaurant_id_to_plate| %>
      <tr>
        <td><%= add_restaurant_id_to_plate.restaurant_id %></td>
        <td><%= link_to 'Show', add_restaurant_id_to_plate %></td>
        <td><%= link_to 'Edit', edit_add_restaurant_id_to_plate_path(add_restaurant_id_to_plate) %></td>
        <td><%= link_to 'Destroy', add_restaurant_id_to_plate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add Restaurant Id To Plate', new_add_restaurant_id_to_plate_path %>
